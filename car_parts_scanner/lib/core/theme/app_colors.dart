import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF07070C);
  static const surface = Color(0xFF0E0E18);
  static const card = Color(0xFF16161F);
  static const border = Color(0xFF24243A);

  // Neon Core Colors
  static const cyan = Color(0xFF22D3EE);
  static const cyanDark = Color(0xFF0EA5E9);
  static const violet = Color(0xFF8B5CF6);
  static const violetDark = Color(0xFF6366F1);

  // POP highlights
  static const lime = Color(0xFFA3E635);
  static const magenta = Color(0xFFF472B6);

  // Role accents
  static const customer = Color(0xFF22D3EE);
  static const customerDark = Color(0xFF0EA5E9);
  static const vendor = Color(0xFFFBBF24);
  static const vendorDark = Color(0xFFF59E0B);
  static const rider = Color(0xFFA78BFA);
  static const riderDark = Color(0xFF7C3AED);
  static const admin = Color(0xFFFB7185);
  static const adminDark = Color(0xFFEF4444);

  // Status colors
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF22D3EE);

  // Text colors (WCAG AA Compliant)
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB4B4CC);
  static const textMuted = Color(0xFF8A8AAB); // Verified 5.4:1 contrast ratio on background
}
